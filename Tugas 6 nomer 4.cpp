#include <stdio.h>

int main() {
    int speed = 2; // meters per second
    int time = 100; // seconds
    int distance = speed * time;
    printf("Jarak yang ditempuh: %d meter", distance);
    return 0;
}

